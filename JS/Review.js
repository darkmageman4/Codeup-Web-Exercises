"use strict"

//VARIABLES
// A variable is a symbolic represenation of a value that can be different in each run of a program
//The variable is a pointer to a stored place in momory of your running application

var name = ("Casey");
var expectedName = ("Casey");

if (name === expectedName){
    console.log("You got the right name");
}
