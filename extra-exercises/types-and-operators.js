(function(){
    "use strict";

    // function isOdd(x){
    //     if (x % 2 !== 0)
    //         return true;
    //     else
    //         return false;
    // }console.log(isOdd(3))

// function isTrue(a,b){
//         return (a * b === 100);
// }console.log(isTrue('2',50))
//
// function isFalse(){
//     return false
// }console.log('Your answer is: ' + isFalse());

// function divide(a,b){
//         return a / b
// }console.log(divide(10,2));

// function multiply(a,b){
//       var product = Number(a) * Number(b)
//     if (isNaN(product)){
//             return false;
//     }else{
//         return product;
//     }
// }console.log(multiply(10,"2"));

// function subtract(a,b) {
//     if ((typeof a === "number" || !isNaN(a)) &&
//         (typeof b === "number" || !isNaN(b)))
//     {
//         return a-b;
//     }else{
//         return false;
//     }
// }
// console.log(subtract("asd",2));

    // TODO: write a function called toUppercase which takes in one input and
    //  returns that input in uppercase IF (and only if) the input is a non-numeric string
    //  otherwise, return false

    // function upperCase(str){
    //     var test = Number(str)
    //     if (test || test === 0 || isNaN(test)) { //
    //         return false;
    //     }else{
    //         return test.toUpperCase();
    //     }
    // }
// console.log(upperCase(null));
//     console.log(Number(undefined))
    // function upperCase(str){
    //     if (typeof str === "string" && isNaN(str))
    //     {
    //         return str.toUpperCase()
    //     }return false
    // }console.log(upperCase('cat'));

// TODO: write a function called stickyCaps which takes in an input and returns that input in sticky caps
//  ie: dog -> dOg, software developer -> sOfTwArE dEvElOpEr
//  -> You may be using a few different string methods
//  -> If the input is numeric or not a string, return false
//     function stickyCaps (kmart)
//     {
//         var target = kmart.toLowerCase()
//         var walmart = "";
//         var i
//         for (i=0; i < target.length; i++)
//         {
//             walmart += i % 2 == 0 ? target.charAt(i).toUpperCase() : target.charAt(i);
//         }
//         return walmart;
//     }
//     console.log(stickyCaps("jungle jim"));









        })()
